# Self-hosted SearXNG endpoint. Public instances are not recommended for agent use.
SEARXNG_URL=https://search.example.com

# Optional. Keep unset unless the instance is protected by a bearer token.
SEARXNG_API_KEY=

